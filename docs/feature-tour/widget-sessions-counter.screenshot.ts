import { defineMetrixWidgetScenario } from '../.screenshots/metrix/widget-scenarios';

export default defineMetrixWidgetScenario({
    slug: 'sessions-counter',
    caption: 'Sessions counter widget with period comparison.',
    intent: 'Show the Sessions counter widget card for dashboard composites.',
});
