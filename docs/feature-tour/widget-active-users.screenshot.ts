import { defineMetrixWidgetScenario } from '../.screenshots/metrix/widget-scenarios';

export default defineMetrixWidgetScenario({
    slug: 'active-users',
    caption: 'Active users realtime counter widget.',
    intent: 'Show the Active users realtime widget card for dashboard composites.',
});
