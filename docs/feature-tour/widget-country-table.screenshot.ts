import { defineMetrixWidgetScenario } from '../.screenshots/metrix/widget-scenarios';

export default defineMetrixWidgetScenario({
    slug: 'country-table',
    caption: 'Country sessions table widget with pagination.',
    intent: 'Show the Country table widget card for dashboard composites.',
});
